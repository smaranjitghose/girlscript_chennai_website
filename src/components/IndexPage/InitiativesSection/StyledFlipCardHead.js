import styled from 'styled-components';

export const StyledFlipCardHead = styled.div`
  font-family: 'Comfortaa', sans-serif;
  font-size: 16px;
  color: var(--theme-color);
  text-transform: uppercase;
  text-align: center;
  letter-spacing: 1.5px;
  margin: 1rem;

  > span {
    display: block;
  }
`;
