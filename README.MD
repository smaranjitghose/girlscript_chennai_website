Backend for GirlScript Website Boilerplate

To run the application :
1. `npm install`
2. Rename `.env.example` to `.env` as fill in the credentials (ask mentors)
3. `npm run start:dev` to start in development mode.

